{
  "name": "iqroku-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local prototype API for IqroKu auth, child profiles, progress, attempts, subscriptions, and assessment.",
  "scripts": {
    "start": "node src/server.mjs",
    "check": "node --check src/server.mjs && node --check src/store.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
